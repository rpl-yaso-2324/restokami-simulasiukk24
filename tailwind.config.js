/** @type {import('tailwindcss').Config} */
module.exports = {
  // Konfigurasi lainnya...

  theme: {
    extend: {
      fontFamily: {
        quicksand: ['Quicksand', 'sans'],
      },
      borderColor: {
        'skyblue': '#bae6fd', 
      },
    },
  },

  
}
